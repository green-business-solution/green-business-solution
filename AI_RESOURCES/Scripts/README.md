# Agent Scripts

Put agent-only helper scripts or notes here when they should not be part of the production `scripts/` directory.

Production ingestion, validation, deployment, routing, and test scripts should stay in `scripts/`.
This folder is for support assets that help agents operate the repo but are not themselves product tooling.
